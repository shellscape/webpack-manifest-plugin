language: node_js
node_js:
  - 'node'
  - '10'
  - '8.3.0'

env:
  - WEBPACK_VERSION=4
  - WEBPACK_VERSION=next

install:
  - npm install
  - npm install webpack@$WEBPACK_VERSION || true

script:
  - npm test

after_script:
  - $(npm bin)/codecov
